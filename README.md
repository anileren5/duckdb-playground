# DuckDB Playground

## Run the container

```bash
docker compose up --build -d
```

## Access Jupyter and notebooks

1. Open [http://localhost:8888](http://localhost:8888)
2. Go to the `notebooks/` folder
3. Open `query_optimization.ipynb`

## Stop the container

```bash
docker compose down
```
